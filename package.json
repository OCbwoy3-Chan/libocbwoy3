{
  "name": "libocbwoy3",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "bun run build",
    "build": "bun run build:all",
    "build:all": "for d in packages/*; do bun run --cwd \"$d\" build; done"
  }
}
